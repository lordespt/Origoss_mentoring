# Subtask: Investigate Some Policy As Code Solutions

## Purpose
Enforce security standards at the cluster level.

## Task Details & Parameters
Deploy a policy engine (e.g., Kyverno) and write a policy that enforces the security contexts applied in the previous step across a specific namespace.

## Acceptance Criteria
- Policy engine blocks the deployment of non-compliant pods in the target namespace.
