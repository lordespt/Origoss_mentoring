# Subtask: Ingress Setup

## Purpose
Expose the web application to the outside of the cluster.

## Task Details & Parameters
Configure an Ingress controller (e.g., NGINX Ingress) and create Ingress resources to access the frontend from the host machine.

## Acceptance Criteria
- Application is accessible via a web browser on the host machine using a local domain or localhost.
