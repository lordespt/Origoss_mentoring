# Subtask: Registry Setup

## Purpose
Set up a local or remote container registry to store the built images.

## Task Details & Parameters
Configure a registry accessible by the local Kubernetes cluster. Push the built frontend and backend images to this registry.

## Acceptance Criteria
- Registry is running and accessible.
- Images can be pushed and pulled successfully.
